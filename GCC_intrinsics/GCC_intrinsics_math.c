#include <math.h>
#include <stdio.h>
int main() {
    double angle = 45.0;
    double sinValue = sin(angle);
    double cosValue = cos(angle);
    printf("cos = %f\n",cosValue);
    printf("sin = %f\n",sinValue);
    return 0;
}